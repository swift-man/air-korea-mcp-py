# 변경 이력

이 파일은 사용자에게 영향을 주는 주요 변경 사항을 기록합니다.
버전은 [유의적 버전 2.0.0](https://semver.org/lang/ko/)을 따릅니다.

## Unreleased

### 추가

- 기존 `AirKoreaService`를 함께 사용하는 FastAPI REST API를 추가했습니다.
- 대기질 OpenAPI 5개 기능과 기준정보를 REST endpoint로도 조회할 수 있습니다.
- 공공데이터포털 계정 및 활용신청 화면으로 연결하는 안내 endpoint를 추가했습니다.
- REST 서버 실행 스크립트와 호스트, 포트 환경변수를 추가했습니다.
- FastAPI 자동 OpenAPI 문서를 `/docs`에서 제공합니다.

### 변경

- REST 서버가 시작할 때 서비스키 설정을 검증하도록 변경했습니다.
- 입력 오류는 `400`, 업스트림 오류는 `502`, 서버 설정 오류는 `503`으로 구분합니다.
- 패키지 설명과 README를 MCP 및 REST 제공 범위에 맞게 갱신했습니다.

### 수정

- Air Korea API가 비정상 JSON 구조나 공공데이터포털 오류 응답을 반환할 때 일반 `500` 오류로 누출되지 않도록 수정했습니다.
- 네트워크 타임아웃을 게이트웨이 오류로 일관되게 처리합니다.
- 빈 REST 포트 환경변수와 테스트 환경변수 격리 문제를 수정했습니다.
- 잘못되거나 필수 필드가 누락된 응답 헤더·본문과 응답 수신 중 연결 중단을 게이트웨이 오류로 처리합니다.
- 공백 서비스키, 잘못된 API 기본 주소, `0` 이하 또는 유한하지 않은 타임아웃을 시작 단계에서 차단합니다.
- 빈 MCP 포트 환경변수를 기본 포트 `8000`으로 처리합니다.

## 0.1.0 - 2026-04-09

### 추가

- 한국환경공단 에어코리아 OpenAPI 5개 기능을 Streamable HTTP MCP 도구로 제공합니다.
- 대기질 예보, 초미세먼지 주간예보, 측정소별 실시간 정보, 통합대기환경지수 나쁨 이상 측정소, 시도별 실시간 정보를 조회할 수 있습니다.
- 원본 `api_payload`, `response_header`, `response_body`와 정규화된 `items`를 함께 반환합니다.
- 예보 이미지 URL을 포함한 Air Korea 원본 응답 필드를 보존합니다.
- 시도 별칭과 고유한 하위 행정구역을 Air Korea `sidoName`으로 변환합니다.
- 모호하거나 지원하지 않는 지역에는 가능한 지역과 재입력 예시를 안내합니다.
- `.env` 기반 실행 스크립트와 systemd 배포 예시를 제공합니다.

### 변경

- MCP 전송 방식을 Streamable HTTP로 단일화했습니다.
- 설정, 게이트웨이, 서비스, MCP 등록 계층을 SOLID 경계에 맞게 분리했습니다.
- 내부망 접속 시 허용할 Host와 Origin을 환경변수로 설정할 수 있습니다.
